version: '3.9'

services:
  app:
    build:
      args:
        RUN_TEST: gradle test
    container_name: testAutomation
    volumes:
      - results:/app/build/reports/tests/test
volumes:
  results:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: C:\criblAssesment\testResult

